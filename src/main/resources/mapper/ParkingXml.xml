<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbfinal.mapper.ParkingMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dbfinal.entity.Parking">
        <id column="pa_id" property="paId" />
        <result column="lot" property="lot" />
        <result column="snum" property="snum" />
        <result column="itime" property="itime" />
        <result column="otime" property="otime" />
        <result column="fee" property="fee" />
        <result column="o_id" property="oId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        pa_id, lot, snum, itime, otime, fee, o_id
    </sql>

</mapper>
